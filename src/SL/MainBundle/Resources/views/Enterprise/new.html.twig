{% extends '::base.html.twig' %}
{% block body %}
<div class="page-header">
	<h1>Enterprise creation</h1>
</div>

<form class="well" action="{{ path('enterprise_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit" class="btn">Create</button>
    </p>
</form>

<div class="row" >
    <div class="record_actions span2 offset1">
        <a class="btn" href="{{ path('enterprise') }}">
            Back to the list
        </a>
    </div>
        </div>{% endblock %}
